# ---------------------------
# CONFIG SERVER CONFIGURATION
# ---------------------------
spring.application.name=api-gateway
# Config Server désactivé (dépendance commentée dans pom.xml)
spring.cloud.config.enabled=false
spring.cloud.config.fail-fast=false

